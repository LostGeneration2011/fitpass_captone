[build]
builder = "dockerfile"
buildArgs = { NEXT_PUBLIC_API_URL = "$NEXT_PUBLIC_API_URL", BACKEND_API_URL = "$BACKEND_API_URL" }

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
